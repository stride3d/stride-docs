> [!NOTE]
> Keep in mind that depending on where you drop the selection, its resulting location might differ:
> 
> * Dropping between two elements of the same parent will move the selection between them (A), while dropping onto an element will make the selection its children (B).
>
>     ![](media/drag-and-drop-behavior-difference.webp)
> 
> * Dropping between two elements, where the first is more nested than the second, will make the selection do something??
>
>     TODO: VISUALIZATION
