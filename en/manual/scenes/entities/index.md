# Entities

Entities create a game's world. They are empty elements that have a collection of components that define what it is (such as a [model component](xref:Stride.Engine.ModelComponent)).

Entities can have **child entities** with their own components.

TODO: VISUALIZATION

Every entity has a [transform component](xref:Stride.Engine.TransformComponent) that defines how it's positioned in relation to it's parent.
