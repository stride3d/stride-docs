# Game settings

You can configure the global settings of your game in the **game settings** asset. By default, it's stored in your main [project package's](../project-packages/index.md) assets folder.

TODO: CHECK IF YOU CAN MOVE IT'S LOCATION WITHOUT STRIDE COMPLAINING

Each system stores it's own settings in a configuration, which you can add or remove in the game settings asset.

## Configurations

Systems in Stride separate their settings into configurations (such as audio or physics). Most need to be added to the game settings asset manually. If a configuration isn't added when running the game, Stride will create a blank one.

You can add a configuration by pressing **➕ Add configuration** and selecting the type of configuration which you want to add.

TODO: IMAGE
